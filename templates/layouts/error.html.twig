{% extends 'base.html.twig' %}

{% block body %}
    <div class="flex justify-center mt-12">
        <div class="flex-col">
            <p class="text-9xl font-bold">
                {{ exception.statusCode }}
            </p>
            <p>{{ exception.statusText }}</p>
        </div>
    </div>
    <div class="flex space-x-2 justify-center mt-12">
        <a href="{{ path('app_post') }}" type="button" class="inline-block px-6 py-2.5 text-white font-medium text-xs leading-tight uppercase rounded shadow-md active:bg-rose-800 bg-rose-600 focus:bg-rose-700 hover:bg-rose-700 dark:active:bg-cyan-800 dark:bg-cyan-600 dark:focus:bg-cyan-700 dark:hover:bg-cyan-700 hover:shadow-lg  focus:shadow-lg focus:outline-none focus:ring-0 active:shadow-lg transition duration-150 ease-in-out">Torna ai post</a>
    </div>
{% endblock %}